Place your background image here named `background.jpg`.

Path: public/background.jpg

Recommended sizes: 1920x1080 or larger. Optimise for web (jpeg/webp) and keep under ~1MB if possible.

If you prefer WebP, name it `background.webp` and update styles accordingly.
